// interface ?
// interface doe what ? and when do I use?


// discribed as
// static constants and abstract methods
// well static constants meaning it has lastly varibles ?
// and abstract methods meaning they arent defined so well? only defined in the function they are used
interface IAccount {
    void Deposit(double Amount);
    void Withdraw(double Amount);
    double GetBalance();
}
